const db = require('./db')
// const {Chair} = require('./models/chair')
// const {User} = require('./models/user')

// Chair.hasMany(User)
// User.hasMany(Chair)

// register models
// require('./models')

module.exports = {
  db
  // Chair,
  // User,
}
